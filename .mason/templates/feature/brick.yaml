name: feature
description: A new brick created with the Mason CLI.

version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.57 <0.1.0"

vars:
  page_name:
    type: string
    description: The name of the page feature module.
    default: my_feature
    prompt: "Enter the name of the feature module"